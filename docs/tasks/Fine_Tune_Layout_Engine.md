# 🎯 Task: Fine-Tune Layout Engine

**Objective:** Enhance the Layout Engine to prevent "orphan" slides (1-2 images) by intelligently recycling images to fill denser layouts, and verify focal point rendering.
**Priority:** High
**Scope:** `src/features/layout/LayoutEngine.ts`, `src/features/composition/SlideLayout.tsx`

---

## 📋 Requirements

### Functional Requirements
- **[REQ-001] Smart Image Recycling**: If a generated slide group has fewer than 3 images (or leads to a boring layout), automatically pad it with random images from the rest of the project to enable richer layouts (like 2x2 Grid or Bento).
- **[REQ-002] Layout Robustness**: Ensure complex layouts (Bento, Mosaic) rendered in `SlideLayout.tsx` handle missing images gracefully, or guarantee they are never assigned if image count is insufficient.
- **[REQ-003] Focal Point Verification**: Confirm that `focalPoint` coordinates ({x, y}) properly translate to CSS `object-position`.

### Technical Requirements
- **[TECH-001]** Maintain pure function properties for `LayoutEngine` (deterministic with seed if possible, though `Math.random` is currently used).
- **[TECH-002]** No new dependencies (use existing utility functions).

---

## 🏗️ Implementation Plan

### Phase 1: Logic Enhancement (LayoutEngine)
- [x] Modify `groupImagesIntoSlides` to:
    - Avoid creating groups smaller than 3 images.
    - If a remainder group is small (e.g., 2 images), either merge with previous, or **pad** with random images from `images` array to reach at least 4.
- [x] Update `selectLayoutForCount` to prioritize denser layouts now that we guarantee more images.

### Phase 2: Component Hardening (SlideLayout) 
- [x] Audit `BentoLayout` and `MosaicLayout`.
- [x] Ensure specific grid cells handle potential `undefined` images (if padding fails) by rendering a placeholder or hiding the cell cleanly.

### Phase 3: Focal Point Check
- [x] Add a temporary visual indicator (e.g., a small red dot) in the Editor Preview for the focal point to visually verify it matches the user's click.

---

## 📁 Files to Create/Modify

| File | Action | Purpose |
|------|--------|---------|
| `src/features/layout/LayoutEngine.ts` | Modify | Implement image recycling logic. |
| `src/features/composition/SlideLayout.tsx` | Modify | Harden layout components. |

---

## ✅ Success Criteria

### Functionality
- [x] No slides with just 1 or 2 images (unless total project has < 3 images).
- [x] The "Bento" layout never has empty white squares.
- [x] Focal points clearly affect image cropping (e.g., face stays in view).

---

## 🔗 Dependencies

**Related files:**
- `src/lib/types.ts`
- `src/stores/editor-store.ts`

---

*Generated by /spawn_task workflow*
